filters:
  and:
    - file.folder.startsWith("Central")
    - file.ext == "md"
    - '!file.tags.contains("Index")'
views:
  - type: list
    name: Index
    groupBy:
      property: categories
      direction: ASC
    order:
      - file.name
      - tags
    markers: none
